/**
 * NaiveUI 组件统一导出
 * 按功能分组，便于按需导入
 */

// 配置组件
export { NConfigProvider, NMessageProvider } from 'naive-ui'

// 布局组件
export { NCard, NSpace, NGrid, NGridItem, NDivider } from 'naive-ui'

// 表单组件
export { 
  NForm, 
  NFormItem, 
  NInput, 
  NInputNumber, 
  NSelect, 
  NSwitch, 
  NCheckbox, 
  NRadio, 
  NRadioGroup 
} from 'naive-ui'

// 按钮和交互组件
export { NButton, NTag, NIcon } from 'naive-ui'

// 反馈组件
export { NAlert, NEmpty, NSpin, NProgress, NModal, NPopconfirm } from 'naive-ui'

// 数据展示组件
export { 
  NList, 
  NListItem, 
  NThing, 
  NStatistic, 
  NCollapse, 
  NCollapseItem, 
  NTabs, 
  NTabPane,
  NText
} from 'naive-ui'

// 上传组件
export { NUpload } from 'naive-ui'

// 滑块组件
export { NSlider } from 'naive-ui'
